class Attacker {
  constructor(x, y) {
    this.x = x;
    this.y = y;
  }
  choseBestMove() {
    // analizes posibilities and choses the best cell to move
  }
  move() {
    // moves 
  }
  draw() {
    // draws the figure
  }
  blockedOrNot() {
    // checks if a posible way of winnig has been blocked by itself. 
  }
}


